# Keystore and signing (never commit)
keystore.properties
*.keystore
*.jks

# Build
/build
app/build
.gradle
local.properties

# IDE
.idea
*.iml

# Local tooling (sprite slicing)
.venv_slice/

# OS
.DS_Store
Thumbs.db

# Large design images (kept locally; not in repo to keep push size small)
Calculator_Screen_Image/
Extra_Image/
App_Sceenshot/

# Keep store_assets/ in the repo (Play Console / marketing graphics — not part of the APK build).
